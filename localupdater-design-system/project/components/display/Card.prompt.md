Container surface for grouping dense UI. Default flat (1px border, no shadow). Pop with `elevation="md"` for floating menus; `selected` adds an amber glow for the active item.

```jsx
<Card padding={20}>…</Card>
<Card interactive elevation="sm">…</Card>
```
