Pill-shaped filter / quick-pick. Selected chip turns amber-tinted with an amber border.

```jsx
<Chip selected leftIcon="folder" count={35}>すべて</Chip>
<Chip leftIcon="cad">CAD</Chip>
<Chip leftIcon="image">画像</Chip>
```

Combine many chips in a horizontal flex row with `gap: 8px`.
