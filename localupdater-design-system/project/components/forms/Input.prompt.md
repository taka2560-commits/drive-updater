Text input — used most commonly for the file-list search box. Pair with `leftIcon="search"` and a Japanese placeholder.

```jsx
<Input leftIcon="search" placeholder="ファイル名で検索…" clearable fullWidth />
```
