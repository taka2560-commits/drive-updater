Full LocalUpdater desktop window. Composes every primitive in the design system. Drop it in and it works — click view-mode tabs, click filter chips, click theme swatches in the sidebar footer to flip themes.

```jsx
<LocalUpdaterApp defaultView="timeline" defaultTheme="night" />
```
